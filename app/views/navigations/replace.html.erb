<% content_for :title, "Replace Navigation" %>

<div class="container">
  <h1 class="d-hotwire-native-none mt-4">Replace Navigation</h1>
  <p>This screen replaced the previous one.</p>
  <p>Trigger this by adding the following to a link:</p>
  <p><code>data: {turbo_action: :replace}</code></p>
</div>
